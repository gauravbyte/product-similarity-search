# Phase 2 — ETL / data-preparation pipeline
pandas>=2.0
numpy>=1.24
pyarrow>=14.0              # parquet output

# Phase 3 — embeddings + similarity engine
sentence-transformers>=2.2  # SBERT text embeddings (pulls torch, transformers)
faiss-cpu>=1.8              # ANN index for fast similarity search (Part 3 bonus)

# Phase 4 — API service
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27                 # required by FastAPI's TestClient
requests>=2.31             # nlq.py — LLM HTTP call
python-dotenv>=1.0         # api/config.py — load LLM_API_KEY from .env
