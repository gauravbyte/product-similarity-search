# Phase 2 — ETL / data-preparation pipeline
pandas>=2.0
numpy>=1.24
pyarrow>=14.0              # parquet output

# Phase 3 — embeddings + similarity engine
sentence-transformers>=2.2  # SBERT text embeddings (pulls torch, transformers)
